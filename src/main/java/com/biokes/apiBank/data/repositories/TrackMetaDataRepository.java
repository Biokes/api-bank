package com.biokes.apiBank.data.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TrackMetaDataRepository extends JpaRepository<TrackMetaDataRepository, Long> {
}
